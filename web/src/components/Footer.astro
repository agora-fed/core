---
import Dialogo from './icons/Dialogo.astro';
import Transparencia from './icons/Transparencia.astro';
import Participacao from './icons/Participacao.astro';
import NewsletterSignup from './islands/NewsletterSignup.svelte';

const year = new Date().getFullYear();
const pillars = [
  {
    Icon: Dialogo,
    name: 'Diálogo',
    color: 'var(--pol-left)',
    text: 'Espaço plural onde diferentes ideias se encontram.',
  },
  {
    Icon: Transparencia,
    name: 'Transparência',
    color: 'var(--pol-right)',
    text: 'Cada resposta — ou silêncio — fica registrado em público.',
  },
  {
    Icon: Participacao,
    name: 'Participação',
    color: 'var(--accent)',
    text: 'Da proposta ao voto, a população no centro da decisão.',
  },
];
---

<footer class="site-footer">
  <div class="container">
    <div class="pillars">
      {
        pillars.map((p) => (
          <div class="pillar">
            <span class="pic" style={`color:${p.color}`} aria-hidden="true">
              <p.Icon size={26} />
            </span>
            <h3>{p.name}</h3>
            <p class="muted">{p.text}</p>
          </div>
        ))
      }
    </div>

    <div class="newsletter">
      <h3>📬 Receba as novidades</h3>
      <p class="muted">
        Acompanhe o placar dos políticos, propostas em alta e o que mudou —
        direto no seu e-mail.
      </p>
      <NewsletterSignup client:visible />
    </div>

    <div class="foot-bar">
      <div class="foot-brand">
        <a href="/" class="brand-line">
          <img src="/emblem.png" width="28" height="26" alt="" />
          <span>DemocraciaBR</span>
        </a>
        <p class="tagline">Diferentes ideias, um só Brasil.</p>
      </div>
      <nav aria-label="Rodapé" class="foot-nav">
        <a href="/politicos">Políticos</a>
        <a href="/propostas">Propostas</a>
        <a href="/f/">Fóruns</a>
        <a href="/consultas">Consultas</a>
        <a href="/servicos">Serviços</a>
        <a href="/cadastrar">Criar conta</a>
      </nav>
      <nav aria-label="Institucional" class="foot-nav institutional">
        <a href="/sobre">Sobre</a>
        <a href="/transparencia">Transparência</a>
        <a href="/privacidade">Privacidade (LGPD)</a>
        <a href="/termos">Termos</a>
      </nav>
    </div>

    <p class="copy muted">
      © {year} DemocraciaBR · Infraestrutura democrática soberana, aberta e
      auditável. Participação sem consequência é teatro.<br />
      Código aberto sob AGPLv3 · IPv6-first · Sem publicidade · Sem venda de dados.
    </p>
  </div>
</footer>

<!-- Plausible (self-hosted em analytics.pop.coop) — pedido do produto 2026-07-26. -->
<script
  is:inline
  defer
  data-domain="democracia.social.br"
  src="https://analytics.pop.coop/js/script.file-downloads.hash.outbound-links.pageview-props.revenue.tagged-events.js"
></script>
<script is:inline>
  window.plausible =
    window.plausible ||
    function () {
      (window.plausible.q = window.plausible.q || []).push(arguments);
    };
</script>

<style>
  .site-footer {
    background: var(--surface-1);
    border-top: 1px solid var(--border-subtle);
    margin-top: var(--sp-12);
    padding-block: var(--sp-10) var(--sp-6);
    transition: background var(--dur-base) var(--ease-out);
  }
  .pillars {
    display: grid;
    gap: var(--sp-5);
    margin-bottom: var(--sp-8);
  }
  @media (min-width: 720px) {
    .pillars {
      grid-template-columns: repeat(3, 1fr);
    }
  }
  .pillar h3 {
    margin: var(--sp-2) 0 var(--sp-1);
    font-size: var(--fs-lg);
    color: var(--text-1);
  }
  .pillar p {
    margin: 0;
    font-size: var(--fs-sm);
    color: var(--text-3);
  }
  .pic {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 46px;
    height: 46px;
    border-radius: var(--r-base);
    background: var(--surface-2);
    border: 1px solid var(--border-subtle);
  }
  .newsletter {
    margin: var(--sp-6) 0;
    padding: var(--sp-4);
    border-radius: var(--r-base, 12px);
    background: var(--surface-2);
  }
  .newsletter h3 {
    margin: 0 0 var(--sp-1);
  }
  .newsletter p {
    margin: 0 0 var(--sp-3);
  }
  .foot-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: var(--sp-6);
    flex-wrap: wrap;
    padding-block: var(--sp-5);
    border-top: 1px solid var(--border-subtle);
  }
  .brand-line {
    display: inline-flex;
    align-items: center;
    gap: var(--sp-2);
    text-decoration: none;
    color: var(--text-1);
    font-weight: var(--fw-semibold, 600);
    font-size: var(--fs-lg);
  }
  .brand-line img {
    display: block;
  }
  .tagline {
    margin: var(--sp-1) 0 0;
    font-size: var(--fs-sm);
    color: var(--text-3);
  }
  .foot-nav.institutional a {
    font-size: var(--fs-xs);
    color: var(--text-3);
  }
  .foot-nav {
    display: flex;
    gap: var(--sp-4);
    flex-wrap: wrap;
  }
  .foot-nav a {
    color: var(--text-2);
    text-decoration: none;
    font-weight: var(--fw-medium);
    font-size: var(--fs-sm);
    transition: color var(--dur-fast) var(--ease-out);
  }
  .foot-nav a:hover {
    color: var(--accent);
  }
  .copy {
    margin: var(--sp-4) 0 0;
    font-size: var(--fs-xs);
  }
</style>
