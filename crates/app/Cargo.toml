[package]
name = "dsoc-app"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Tier-0 wiring contract: the shared AppState every crate's routes() receives. Bridges core ports and the Db pool."

[lints]
workspace = true

[dependencies]
dsoc-core = { path = "../core" }
dsoc-db = { path = "../db" }
axum = { workspace = true }
uuid = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }
